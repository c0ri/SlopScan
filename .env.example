# Optional. Raises SlopScan's own GitHub API rate limit for the repo-richness
# signal from 60/hr (unauthenticated) to 5000/hr. No scopes needed — read-only
# access to public repo metadata is all it uses.
GITHUB_TOKEN=
